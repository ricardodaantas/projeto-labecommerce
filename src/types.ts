export type Tuser = {
  id: string;
  name: string;
  email: string;
  password: string;
  createdAt: string;
};

export type Tproduct = {
  id: string;
  name: string;
  price: number;
  description: string;
  image_url: string;
};
